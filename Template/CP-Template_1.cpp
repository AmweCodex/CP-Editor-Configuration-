#include <bits/stdc++.h>
using namespace std;

class Solution {
   public:
    void solve() {
        // Write your code here.
    }
};

auto init = []() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    return 0;
}();

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t = 1;  // test cases
    // cin >> t;

    Solution sol;
    while (t--)
        sol.solve();

    return 0;
}
